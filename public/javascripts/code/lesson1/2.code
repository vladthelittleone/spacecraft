/**
 * Функция run используется для управления кораблем.
 * Вызывается в бесконечном цикле.
 */
this.run = function(transport)
{
	// Команда кораблю - поворачивать налево.
	transport.rotateLeft();

	// Добавьте команду на следующую строку.

};
