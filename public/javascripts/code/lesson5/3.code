var a;
// Приоритет оператора определяет
// то в каком порядке будет выполняться выражение
a = 10 + 12 * 3 / 2;
BBotDebug(a);

// Все операторы имеют одинаковый приоритет, а значит будут выполнены
// последовательно слева направо
BBotDebug(10 + 12 + 3 + 2);
