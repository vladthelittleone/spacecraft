/**
 * Что за странный код?
 * run?
 * Что это?
 * Что мне делать?
 */
this.run = function(spaceCraft)
{
	// С помощью + можно объединять несколько строк в одну.
	// Данный оператор называется конкатенацией.
	BBotDebug(spaceCraft.getX() + ' ' + spaceCraft.getY());
};
