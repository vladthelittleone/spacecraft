/**
 * Академия будет уничтожена!
 * Это лишь предупреждение!
 */
this.run = function(transport)
{
	// Команда отправляет корабль в точку 1100, 1100
	// в координатах мира.
	transport.moveToXY(1100, 1100);
};

